# 监考环境变量配置

# AI监考WebSocket服务器地址
VITE_PROCTOR_WS_URL=ws://localhost:8000/ws/monitor
# 监考 REST API 地址（身份注册等）
VITE_PROCTOR_API_URL=http://localhost:8000

# 是否启用监考功能
VITE_PROCTOR_ENABLED=true

# 帧发送间隔（毫秒）
VITE_PROCTOR_FRAME_INTERVAL=1000

# 最大违规次数
VITE_PROCTOR_MAX_VIOLATIONS=10
